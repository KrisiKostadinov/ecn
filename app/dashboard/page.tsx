import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Табло",
};

export default async function Dashboard() {
  return (
    <div>
      <div>Dashboard</div>
    </div>
  );
}
